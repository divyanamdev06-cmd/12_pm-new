# Copy to .env and set values locally.
JWT_SECRET=use-a-long-random-string-in-production
JWT_EXPIRES_IN=7d

# Optional: Mongo connection (defaults are in config/db.js if you wire them)
# MONGODB_URI=mongodb://localhost:27017/jobnest
